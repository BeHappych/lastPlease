definitions:
  main.List:
    properties:
      address:
        type: string
      birthday:
        type: string
      full_name:
        type: string
      id:
        type: string
    type: object
host: localhost:8080
info:
  contact: {}
  description: Swagger API for Golang Project.
  title: Swagger API
  version: "1.0"
paths:
  /lists:
    get:
      consumes:
      - application/json
      description: get lists
      parameters:
      - description: User ID
        in: body
        name: lists
        required: true
        schema:
          $ref: '#/definitions/main.List'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/main.List'
            type: array
      summary: List tables
      tags:
      - lists
swagger: "2.0"
